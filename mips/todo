on method labels, update the pointers, push the methodSize on localAllocated stack

on methodEnds, pop from LA stack

add exprs

how to pass args to methods
waths is fp



assign (=)

var
nil
true
false
char
int

float - ??

exponent (l3_expr) - left because no mips instr present for it
